export const mutations = {
  setCouples: (state, couples) => {
    state.couples = couples;
  },
  setGraphData: (state, graphData) => {
    state.graphData = graphData;
  },
  setGraphPrediction: (state, graphPrediction) => {
    state.graphPrediction = graphPrediction;
  },
};
